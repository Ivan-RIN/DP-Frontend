const state = {
	dataTypes:
		{
			number: 'number',
			date: 'date',
			string: 'string',
			objectType: 'object',
		},
};

const getters = {
	dataTypes: state => state.dataTypes,
};

const actions = {
};

const mutations = {
};

export default
{
	state,
	getters,
	actions,
	mutations,
};
